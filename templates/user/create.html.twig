{% extends 'base.html.twig' %}

{% block title %}
	Add New User
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 1100px;
			width: 100%;
			font: 18px / 1.5 sans-serif;
		}
		.btn {
			border: none;
			color: white;
			padding: 8px 15px;
			text-align: center;
			text-decoration: none;
			display: inline-block;
			font-size: 15px;
			margin: 2px 2px 2px 5px;
			cursor: pointer;
		}
		.btn4 {
			background-color: black;
			margin-bottom: 1rem;
		}
	</style>

	<div class="example-wrapper">
		{# <a href="{{absolute_url(asset(''))}}all_users" class="btn btn4 ">Back</a> #}
		<a class="btn btn4" href="{{ path('all_users') }}"> Back </a>

		<h1>Create New User </h1>

		{{ form_start(form) }}
		{{ form_widget(form) }}
		{{ form_end(form) }}

	</div>
{% endblock %}
